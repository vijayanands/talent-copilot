from auth.auth import initialize
from helpers.github import get_pull_request_info, get_pull_request_by_author

# Press the green button in the gutter to run the script.
if __name__ == "__main__":

    # initialize
    initialize()

    # username = "michael-s-molina"
    # repo_owner = "apache"
    # repo_name = "superset"
    # get_pull_request_info(username, repo_owner, repo_name)

    # username = "betodealmeida"
    # repo_owner = "apache"
    # repo_name = "superset"
    # get_pull_request_by_author(username, repo_owner, repo_name)
